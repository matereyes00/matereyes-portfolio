# CI/CD Documentation Generation with Docusaurus

This repository uses **GitHub Actions** to automatically generate and optionally deploy Docusaurus documentation whenever code is pushed to the `main` or `master` branches.

## Setup

### 1. Install Dependencies

```bash
npm install
```
