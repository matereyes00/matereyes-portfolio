name: Generate Documentation

on:
    push:
        branches:
            - develop
        paths:
            - "src/**"
            - "package.json"
            - "docs/**"
    pull_request:
        branches:
            - develop

jobs:
    generate-docs:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "20"
                  cache: "npm"

            - name: Install dependencies
              run: npm install

            - name: Build Docusaurus docs
              run: npm run docs:build

            - name: Deploy Docusaurus docs
              if: github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/develop'
              run: |
                  npm install -g gh-pages
                  # Set Git user identity so gh-pages can commit
                  git config --local user.email "action@github.com"
                  git config --local user.name "GitHub Action"
                  # Deploy docs/build to gh-pages branch
                  gh-pages -d docs/build -r https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
